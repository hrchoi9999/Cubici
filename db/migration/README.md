# DB Migration Tools

Tools for converting legacy MySQL schema/data to PostgreSQL.

Rules:

- Do not print raw personal/payment/bank/card values in logs.
- Verification reports should use counts, sums, and IDs only when needed.
- All source and target files stay inside `D:\Alt_CSM\Cubici`.

