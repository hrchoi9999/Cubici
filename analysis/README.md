# Cubici Analysis

Python analysis workspace for synthetic transaction data, scoring models, charts, Excel exports, and report prototypes.

Install dependencies from the workspace root:

```powershell
pip install -r analysis\requirements.txt
```

Only anonymized or synthetic data should be written to `data_sample`.

Generate baseline sample CSV files:

```powershell
python analysis\generate_sample_data.py
```
