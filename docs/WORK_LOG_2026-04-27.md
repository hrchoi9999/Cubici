# Cubici Work Log - 2026-04-27

## Summary

Today we prepared the Cubici local development/demo environment so this PC can temporarily serve as the backend and DB server for external demonstrations.

## Completed

- Restored and verified the local MySQL database.
- Confirmed MySQL runs on `127.0.0.1:3307`, database `cubici`.
- Built and ran the Spring Boot backend from `cubici_redem`.
- Added safe backend APIs:
  - `GET /api/health`
  - `GET /api/dashboard/summary`
- Connected the website dashboard script to backend data.
- Confirmed dashboard summary values from the restored DB:
  - Sales total: `67,620,025`
  - Settlement deposit amount: `61,554,507`
  - Moneybank service balance: `913,604`
- Added local server helper scripts:
  - `scripts/local-server-start.cmd`
  - `scripts/local-server-status.cmd`
  - `scripts/local-server-stop.cmd`
  - backend-only start/status/stop scripts
- Installed `cloudflared` under `private_local`.
- Created Cloudflare Tunnel:
  - Tunnel name: `cubici-local-api`
  - Public API URL: `https://api.cubici.co.kr`
  - Local origin: `http://127.0.0.1:18080`
- Added tunnel helper scripts:
  - `scripts/tunnel-start.cmd`
  - `scripts/tunnel-status.cmd`
  - `scripts/tunnel-stop.cmd`
- Confirmed external API access:
  - `https://api.cubici.co.kr/api/health`
  - `https://api.cubici.co.kr/api/dashboard/summary`
- Updated the deployed website script so `cubici.co.kr` uses `https://api.cubici.co.kr` by default.
- Confirmed `https://cubici.co.kr` serves the updated backend script.

## Git Commits

Main repository:

- `1cdf90d` - Connect dashboard to local backend data
- `10f0174` - Prepare production API deployment
- `8d8e39a` - Add local backend DB server scripts
- `86874a6` - Add Cloudflare tunnel demo setup

Backend repository `cubici_redem`:

- `e70df1a` - Add safe health endpoint
- `46eba7a` - Add dashboard summary endpoint
- `5fa6d0d` - Restrict API CORS origins

Backend repository commits are local only because its remote still points to the old retired server.

## Demo Startup Commands

Start local DB and backend:

```bat
C:\Cubici\scripts\local-server-start.cmd
```

Start Cloudflare Tunnel:

```bat
C:\Cubici\scripts\tunnel-start.cmd
```

Check status:

```bat
C:\Cubici\scripts\local-server-status.cmd
C:\Cubici\scripts\tunnel-status.cmd
```

Stop after demo:

```bat
C:\Cubici\scripts\tunnel-stop.cmd
C:\Cubici\scripts\local-server-stop.cmd
```

## Important Notes

- This PC must stay powered on and connected to the internet during external demos.
- MySQL is not exposed to the internet.
- Cloudflare Tunnel exposes only the backend API.
- Private credentials are stored outside Git:
  - `C:\Users\PC\.cloudflared\`
  - `C:\Cubici\private_local\`
- Do not commit DB dumps, credentials, API keys, or Cloudflare tunnel credential files.

## Next Work

The next phase is PRIZM v2 planning:

- Define investor demo screens.
- Design PRIZM v2 database tables.
- Plan Python data collection and feature engineering pipeline.
- Add backend APIs for score, risk signals, evidence data, and trends.
- Apply the current Cubici design system to PRIZM demo pages.
